# 2026-04-25-011 片段合并 + 全程height:1.6，对齐问题仍未解决

## 操作

1. `buildTextSpan` 新增相邻同样式片段合并（`styleEquals` + `mergedWith`），减少文本运行数
2. `baseStyle.copyWith(height: 1.6)` 作为父级，所有子 span 继承相同 height
3. 保持 `StrutStyle(forceStrutHeight: true, height: 1.6)`

## 结果

删除线位置和涂色背景高度不一致的问题仍未解决。

## 状态

已落档至 `.ai/dev/dev_last.md`，暂停尝试，转由其他 AI 处理。
