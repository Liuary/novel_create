# 2026-04-25-015 代码审查与修复

## 审查对象

`phase1-polish` 分支（基于 `master`），包含 15 个提交。

## 审查范围

| 文件 | 行数 | 评估 |
|------|------|------|
| `lib/widgets/editor_page.dart` | 1439 | 通过（2个问题已修复） |
| `lib/widgets/sidebar.dart` | 710 | 通过 |
| `lib/services/toast_service.dart` | 50 | 通过（定时器泄漏已修复） |
| `lib/models/user_config.dart` | 49 | 通过 |
| `lib/providers/app_providers.dart` | 190 | 通过（renameChapter刷新已修复） |
| `lib/pages/home_page.dart` | 93 | 通过 |
| `lib/widgets/toast_overlay.dart` | 64 | 通过 |

## 发现并修复的问题

1. **标注脏状态追踪** — `toString()` 无法正确序列化 `List<Annotation>`，改为 `jsonEncode`
2. **ToastNotifier 定时器泄漏** — `Notifier` 无自动 dispose，添加 `ref.onDispose`
3. **renameChapter UI 不刷新** — 缺少 `ref.invalidateSelf()`

## 验证

- `flutter analyze`: No issues found
- 用户手动测试：正常

## 合并结论

`phase1-polish` → `master`，无阻断问题。
