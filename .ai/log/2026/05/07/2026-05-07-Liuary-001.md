# 公共日志 — 2026-05-07-Liuary-001

> 时间: 2026-05-07 21:19
> 类型: 代码审查 - 验收

## 验收摘要

对第三轮代码审查 18 条问题进行验收。代码 Agent 处理了 16 条（REV-011 和 REV-018 尚未处理）。

### 验收结果

| 结论 | 数量 | 条目 |
|------|------|------|
| ✅ 通过 | **14** | REV-002~006, 008~010, 012~017 |
| ❌ 不通过 | **2** | REV-001, REV-007 |
| ⏸️ 未处理 | **2** | REV-011, REV-018 |

### 不通过详情

1. **REV-001**（high）：`app_providers.dart` 方法签名已改为 `Future<void>` + 内部 `await`，但 9 处调用方仍使用 `unawaited()` 而非 `await`，实质仍是 fire-and-forget。
2. **REV-007**（medium）：`outline_node_editor.dart:98` `_save()` 在 `await widget.repo.update()` 后缺少 `if (!mounted) return;`，直接调用 `widget.onTitleChanged()`。

### 关键修复亮点

- REV-005（copyWith null传递）：两个模型均使用 `_sentinel` 哨兵值模式，设计规范
- REV-013（EntityLink去重）：使用 `getSingleOrNull` 做 5 元组唯一性检查
- REV-017（并行初始化）：`ModuleRegistry.initializeAll` 改为 `Future.wait`
