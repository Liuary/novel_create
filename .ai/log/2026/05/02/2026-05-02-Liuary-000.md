# 操作日志 — 2026-05-02-Liuary-000

## 概述

大纲模块打磨 + 卷/章/大纲统一拖拽排序 + 知识库编辑器集成 + 可视化预览 + 多项交互优化

## 完成内容

### 1. 大纲模块完善
- 状态中文化（规划中/写作中/已完成）
- `outline_repository.dart`：getAll/getRoots/getChildren 多态bookId过滤；新增 reorderSibling / swapWithAdjacent
- `_OutlineTreeNav` → ConsumerStatefulWidget，从 Provider 直接读取 bookId
- addRoot/addChild 支持可选 title/bookId 参数；自增命名 `findAvailableName`
- F2 重命名 + 回车确认 + 同局部排序

### 2. 拖拽排序
- `dragModeProvider` 开关（侧边栏 Tab 栏右侧按钮）
- 卷：`_BookVolumeChapterTree` → ConsumerStatefulWidget + 本地 `_volumes` 状态；拖拽模式 `ReorderableListView` + `ReorderableDragStartListener` leading 手柄
- 章：`_ChapterList` → ConsumerStatefulWidget + 本地 `_chapters` 状态；同上拖拽模式
- 大纲：拖拽模式 `ReorderableListView` 平面树 + `ReorderableDragStartListener`；sibIndex 同名节点间非连续映射修正；`_applySiblingReorder` 本地同步更新 + sort 重排
- `_moveVolumeUp/Down` / `_moveChapterUp/Down`（右键菜单专用 swap）
- 移除所有重排方法中错误的 `(old < new) new--` 递减

### 3. 可视化预览
- `outline_visual_preview.dart`：横版分支图 + InteractiveViewer 拖拽滚动
- CustomPainter 绘制中央水平线 + 交替上下分支 + 节点卡片（标题/类型/状态/字数/描述）
- 双击导航 + 返回上级（breadcrumb + 父节点查询）
- 节点描述单行截断显示

### 4. 编辑器集成
- `activeKnowledgeModuleIdProvider` → 侧边栏选中模块 → 主区域显示 `module.buildEditor()`
- `_OutlineEditorHost` 详情/可视化切换（_ToggleChip）
- `EditorPage.deactivate` 捕获 notifier 引用避免 unmounted ref 错误

### 5. 交互修复
- 重命名后 `_loadNodes` 改为本地更新标题
- `TreeNavPanel` 右键菜单根据 dragMode 隐藏上移/下移
- `use_build_context_synchronously` 修复
- `DropdownButtonFormField.value` → initialValue + ValueKey

## 文件变更

**新建**：`lib/modules/outline/widgets/outline_visual_preview.dart`

**修改**：`lib/core/database/app_database.dart`, `lib/core/module/module_context.dart`, `lib/main.dart`, `lib/pages/home_page.dart`, `lib/providers/app_providers.dart`, `lib/widgets/editor_page.dart`, `lib/widgets/sidebar.dart`, `lib/modules/outline/outline_module.dart`, `lib/modules/outline/outline_repository.dart`

**flutter analyze**：零问题
